---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: challengeinstances.kubeflag.io
spec:
  group: kubeflag.io
  names:
    kind: ChallengeInstance
    listKind: ChallengeInstanceList
    plural: challengeinstances
    shortNames:
    - instance
    - instances
    singular: challengeinstance
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Duration for the challenge instance
      jsonPath: .spec.ttl
      name: Duration
      type: string
    - description: Challenge Reference
      jsonPath: .spec.challengeRef
      name: Challenge REF
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChallengeInstance represents an instance of a challenge
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              challengeRef:
                type: string
              team:
                type: string
              ttl:
                type: string
              user:
                type: string
            required:
            - challengeRef
            - team
            - user
            type: object
          status:
            properties:
              active:
                type: boolean
              expirationTime:
                format: date-time
                type: string
            required:
            - active
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
